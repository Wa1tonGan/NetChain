# NetChain 全面实施计划（已获用户口头批准，重新提交执行）

用户已明确说「implement all the phase」，此计划为之前 v2 计划的原样执行版本：

## Phase 0 — 卫生整改
- `.gitignore` 加 `fixtures/keys/*.private.pem`，`git rm --cached` 移出跟踪（不改历史）
- `providerAgent.js` 补 CORS（access-control 头 + OPTIONS preflight）

## Phase 1 — 动态 Provider per-request
- `dynamicProviders.js` 加 `derivePersonasForIncident(base, incidentId)`（seed=hash(incidentId) 确定性人设）
- `providerAgent.js` 报价时按 `(incidentId, providerId)` 现场推导；`rescueAgent.js` 同步推导
- 同 incident 重试同人设；不同 incident 全新市场；keys/端口/providerId 不变

## Phase 2 — ServicesPage 实时 provider 市场面板
- 新 `services/market.ts` 轮询 8101–8103 `/agent-card` + `/health` + gateway `/readiness`
- 市场卡片 + 一键 `POST /admin/mode` 杀 provider + 展示「身份=收款地址」

## Phase 3 — 全链路移植（integrationof123 cheat sheet）
- `services/live.ts`（BRANDS 改从 `/readiness` 拉）+ types 扩展 + `flows.ts` live flow
- `useAppStore.ts`：`startLiveRecovery` + live machine（beginLive/onGatewayEvent/adoptSelectedOffer/markAvailable/onChainRow/finishLive/finishNoRecovery）
- DevPage Live 卡；HomePage LIVE/SIM 切换 + 真实事件流；RecoveryOverlay live 适配；DecisionCard 真实 comparison

## Phase 4 — ActivityPage 链上事件
- store `chainRows` + TRUST 8200 SSE `/v1/events`（断线重连）
- ActivityPage 事件区（真实 txDigest → suiscan 链接）；DetailPage proofs 用真实 commitTx

## Phase 5 — ProfilePage 真实余额
- 加 `@mysten/sui` 依赖 + vite proxy `/suirpc` → testnet fullnode
- `services/wallet.ts` 查 zkLogin 地址余额（10s 轮询）；有会话显示链上余额（标 on-chain），否则回退 demo

## Phase 6 — Truth Agent 改版
- 后端：`GET /claims/demo`（最近真实运行快照，标 replayOf）
- 前端重写：4 项 checklist 需求面板随步骤点亮、思考管线步骤图、模型并行卡片、半圆 gauge、推理账本、REPLAY 回放（删掉本地随机生成）

## Phase 7 — zkLogin 买家
- **7a Move**：`commit_as_buyer`（无 cap + Coin 直付锁仓），复用现有检查（双验签/expiry/nonce/fee），平台 commit 保留；Move 测试：成功/坏签名/过期/重放；`sui move build + test`
- **7b Trust server**：`/v1/commit` 加 `submit:false`（build-only 返回 txBytes，走 commit_as_buyer）；`POST /v1/commit/confirm` 收 txDigest 入 ledger；settle/refund/verify 改 `PLATFORM_SECRET` env；buyer key 退出
- **7c Bridge+前端**：authorize 加 nonce（ephemeral pubkey）、callback 返回 salt+idToken、新增 `/api/zklogin/prove` 代理 prover；前端 zk 会话（sessionStorage ephemeral keypair + proof + maxEpoch）+ `zkSignAndSubmit`；资金冷启动（平台 key mint/转 MYRC 到用户地址 + sponsored gas，复用 gasless.js/sponsored.js）；`ZK_SIGNING=false` fallback（per-session 服务端派生 key，UI 标注 custodial）；A2A buyer agreement 保持平台签，短信确认=consent 点

## 验证
- 每 Phase：`tsc --noEmit && vite build`；全量 `node --test`（基线 79/5 不得变差）；`sui move build + test`
- 冒烟：per-request 人设两个 incident 不同；live 全链路；Truth REPLAY；zk commit testnet（如网络可用）
- 完成后输出中文总结（含给 Person 3 的适配清单）