# Handoff — NetChain Person 3 (Sui & Reliability)

**Session date:** 2026-08-30 · **Repo:** `C:\repository\NetChain`, branch `herman`, tip `52c8d2b`, pushed · **Deadline:** Devfolio 2026-09-05 23:59 MYT, pitch 09-06.

## Read these first (do not duplicate, reference them)

- `documents/person3/person3-implementation-guide.md` — THE walkthrough (short + detailed): architecture, repo map, money rules, traps §11, integration points §12.
- `documents/person3/person3-trust-contract.md` — wire-level contract incl. fee-split + verification loop + fixtures-policy note to P2.
- `README.md` — "Proof transactions — every use case, once" table: every digest for the Buyer 1 USDC era (lock / verify / complete / refund / failover+penalty / gasless-0-gas / sponsored).
- `documents/blueprint.md` — Person 3 scope = §8 col 3, §12 acceptance; the team is hacking BOTH Sui tracks (01 Payments & Stablecoins, 02 AI × SUI).
- Git log on `herman` — every milestone is a descriptive commit (judge-facing artifact).

## Current state (all green, all proven on testnet)

- **Two-track build complete and proven with REAL Circle USDC** signed by **Buyer 1** (a real self-custody wallet): 19/19 Move tests, 83/83 offline repo tests (incl. Person 2's agent-runtime tests), **13/13 harness checks on testnet**, full demo + `npm run payments:sui` each use case exactly once.
- Live package `0xa648e1fbb1ae8f04…35bf`; shared `Escrow<USDC>` pool; platform fee wallet `0xabc67fa…c8f4` collects fees; Buyer 1 = `0xbed2dff8…758cf0` (~37.5 USDC + 1.13 SUI left after the proof round).
- Asset = config (`src/sui/stablecoin.js`): localnet MYRC 2 decimals (sen), testnet USDC 6 decimals; money of record = integer base units; testnet fixtures price-scaled (`SUI_TESTNET_PRICE_SCALE=0.05`, `STABLECOIN_ESCROW_FUND=12` in gitignored `.env`).
- Payment rails: gasless (`balance::send_funds`, gas 0, zero-SUI sender) + sponsored (customer signs, platform gas wallet pays) — both proven, digests in README.
- Voucher fee = the buyer-SIGNED `agreement.platformFee` (P2's fee-split agreement shape); the service never recomputes.
- MYRC = 2 decimals (sen) — P2's signed fee 5.25 on the 105 fallback was unpayable on a 0-decimal coin (found + fixed this session).
- Fixtures policy: `fixtures/sui/` + `.DS_Store` are **gitignored** (per-run generated, 5-min TTL — the generator `scripts/sui-fixtures.mjs` is the source of truth; demo/harness/tests regenerate). `fixtures/selected/` + `fixtures/providers/` stay committed. P2 informed in the contract doc.
- Merged main (0322c22) + re-integrated: the merge had taken main's side for voucher.js/sui-fixtures.mjs/sui-demo.mjs — the asset layer was rebuilt on P2's new shapes. P2's new runtime (`src/agents/` rescue+provider agents, gateway :8082) is in the repo, NOT yet wired to us.

## Wallets (user-managed; NEVER commit keys)

- Platform/fee wallet `0xabc67fa…c8f4` — receive-only, no key in code.
- Buyer 1 `0xbed2dff8…` — default testnet buyer; key in gitignored `.env` as `SUI_BUYER_SECRET` (pasted in chat 2026-08-30 — testnet-only, rotate before real value). Gas via `node --env-file-if-exists=.env scripts/fund-buyer-gas.mjs <addr>` (signs with the legacy PEM buyer 0x016dcf…, which holds ~55 USDC + spare SUI on testnet).
- Buyer 2 `0x499d7303…` — spare (faucet when needed). zkLogin real-UI = Person 1's scope.

## Next session's work, in order

1. **Video clips** (team deliverable): README digest table is the shot list — lock → duplicate-block → verify → split settlement → refund → penalized failover → gasless-0-gas → sponsored. Localnet for unlimited retakes (`sui start --with-faucet --force-regenesis`, `rm .sui/config.localnet.json`, `npm run sui:setup`); testnet digests as the "real" closer.
2. **PR herman→main** via browser (no `gh` CLI): https://github.com/Wa1tonGan/NetChain/compare/main...herman — frame for BOTH tracks.
3. **Walrus voucher archive** (optional, Track-02 feature, time-boxed 90 min): `npm i @mysten/walrus` + WAL testnet faucet → store voucher + §9 connection-log JSON per settlement → blob ID into an `ARCHIVED` ledger event + reliability report. Fallback: leave documented as future work.
4. **Buffer:** wire P2's live runtime (`src/agents/` gateway :8082) via our ready hooks — `commitFromUrl` (pull), `postSettlement`/`P2_CALLBACK_URL` (callback), SSE :8200 for Person 1's dashboard.
5. **Rehearsal:** `SUI_NETWORK=testnet SUI_REUSE_POOL=1 npm run demo:sui` after a fresh `sui:setup` pool (a reused pool's nonces are spent — fresh pool per full run, or nonces replay-abort).

## Traps (already solved — do not rediscover)

- `tx.balance({type, balance})` sources from address balance AND coins (Circle faucet USDC lands as either); `executeTransaction` takes raw BCS **bytes** not base64 (protobuf "invalid uint 32" trap); sponsorship = `setGasOwner` + `setGasPayment` + two signatures.
- Event queries: gRPC `listEvents({filter:{emitModule:"pkg::escrow"}})` (old MoveModule shape rejected); `vector<u8>` event fields arrive base64 (ledger stores hex).
- gRPC indexing lag after txs → `waitForObject`/`waitForTransaction` are wired, don't remove.
- Offline suite: `--test-concurrency=1` + forced-MYRC fixture regen in `test/sui-trust.test.js` (the last on-chain run otherwise leaves USD fixtures that break offline tests).
- Localnet validator OOMs after hours — restart + `rm .sui/config.localnet.json` + `sui:setup`. Never mainnet (DQ).
- Sensitive values (Buyer 1 key, sponsor key) live ONLY in gitignored `.env` / `.sui/` — never commit, never paste in new sessions.
- Post-merge recovery: if a merge resolves our files to main's side, diff against the pre-merge herman commit (`git diff cab0f02 525e799` was this session's baseline) and re-apply on top of main's new shapes.

## Suggested skills

- `superpowers:executing-plans` — step-by-step execution mode used all session.
- `superpowers:test-driven-development` — for any new code paths.
- `superpowers:systematic-debugging` — if testnet misbehaves.
- `superpowers:verification-before-completion` — before claiming any acceptance done.
- `superpowers:finishing-a-development-branch` — for the PR/merge step.
- `superpowers:using-superpowers` — entry point if any other skill name is unclear.
