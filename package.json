{
  "name": "netchain-person-1",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "provision": "node scripts/provision-demo-identity.mjs",
    "generate:fixtures": "node scripts/generate-fixtures.mjs"
  },
  "dependencies": {
    "zod": "^4.0.0"
  }
}
